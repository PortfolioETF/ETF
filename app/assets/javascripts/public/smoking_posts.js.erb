 <%# Place all the behaviors and hooks related to the matching controller here. %>
 <%# All this logic will automatically be available in application.js. %>
 <%# You can use CoffeeScript in this file: http://coffeescript.org/ %>

function postFormDisplay() {
  var formPost = document.getElementById('form-post');
  var saveMyLocationButton = document.getElementById('save-my-location-button');
  formPost.style.display = 'block';
  saveMyLocationButton.style.display = 'none';
};
